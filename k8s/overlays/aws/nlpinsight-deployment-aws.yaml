# NLPInsight Analyzer Deployment — AWS EKS
# Mirror of k8s/nlpinsight-deployment.yaml (GCP GKE version)
# Cloud-specific differences: ECR image registry, S3 init containers (boto3), IRSA auth
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nlpinsight-analyzer
  namespace: ml-portfolio
  labels:
    app: nlpinsight-analyzer
    tier: ml-service
    version: v3.5.0
    cloud: aws
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: nlpinsight-analyzer
  template:
    metadata:
      labels:
        app: nlpinsight-analyzer
        tier: ml-service
        version: v3.5.0
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: ml-workload
      containers:
      - name: nlpinsight-api
        image: 531948420830.dkr.ecr.us-east-1.amazonaws.com/ml-portfolio/nlpinsight:latest
        imagePullPolicy: IfNotPresent
        command: ["uvicorn", "app.fastapi_app:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "2"]
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP
        env:
        - name: MODEL_PATH
          value: "/app/models"
        - name: ARTIFACTS_DIR
          value: "/app/artifacts"
        - name: PYTHONUNBUFFERED
          value: "1"
        - name: LOG_LEVEL
          value: "INFO"
        - name: LOG_FORMAT
          value: "json"
        - name: SERVICE_NAME
          value: "nlpinsight"
        - name: API_ROOT_PATH
          value: "/nlpinsight"
        envFrom:
        - configMapRef:
            name: nlpinsight-config
        resources:
          requests:
            memory: "512Mi"
            cpu: "300m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
        volumeMounts:
        - name: models
          mountPath: /app/models
        - name: data
          mountPath: /app/data
      initContainers:
      - name: download-model
        image: python:3.11-alpine
        command:
        - sh
        - -c
        - |
          pip install --quiet boto3==1.35.0 && \
          python /scripts/download-model.py
        envFrom:
        - configMapRef:
            name: nlpinsight-model-config
        volumeMounts:
        - name: models
          mountPath: /models
        - name: download-script
          mountPath: /scripts
      - name: download-data
        image: python:3.11-alpine
        command:
        - sh
        - -c
        - |
          pip install --quiet boto3==1.35.0 && \
          python /scripts/download-model.py
        envFrom:
        - configMapRef:
            name: nlpinsight-dataset-config
        volumeMounts:
        - name: data
          mountPath: /data
        - name: download-script
          mountPath: /scripts
      volumes:
      - name: models
        emptyDir: {}
      - name: data
        emptyDir: {}
      - name: download-script
        configMap:
          name: download-script
          defaultMode: 0755
---
apiVersion: v1
kind: Service
metadata:
  name: nlpinsight-service
  namespace: ml-portfolio
  labels:
    app: nlpinsight-analyzer
spec:
  type: ClusterIP
  selector:
    app: nlpinsight-analyzer
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nlpinsight-config
  namespace: ml-portfolio
data:
  MODEL_VERSION: "v3.0.0"
  FEATURE_VERSION: "v1"
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: nlpinsight-hpa
  namespace: ml-portfolio
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: nlpinsight-analyzer
  minReplicas: 1
  maxReplicas: 3
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 75
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Percent
        value: 50
        periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
      - type: Percent
        value: 100
        periodSeconds: 30
      - type: Pods
        value: 2
        periodSeconds: 30
      selectPolicy: Max
