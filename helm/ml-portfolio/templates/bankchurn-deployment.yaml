{{- if .Values.bankchurn.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bankchurn-predictor
  namespace: {{ .Values.namespace }}
  labels:
    app: bankchurn-predictor
    {{- include "ml-portfolio.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.bankchurn.replicas }}
  selector:
    matchLabels:
      app: bankchurn-predictor
  template:
    metadata:
      labels:
        app: bankchurn-predictor
    spec:
      initContainers:
        - name: download-model
          image: {{ .Values.modelStorage.initImage }}
          command: ["python", "/scripts/download-model.py"]
          env:
            - name: GCS_BUCKET
              value: {{ .Values.modelStorage.bucket }}
            - name: GCS_MODEL_PATH
              value: {{ .Values.bankchurn.model.gcsPath }}
            - name: LOCAL_MODEL_PATH
              value: {{ .Values.bankchurn.model.localPath }}
          volumeMounts:
            - name: model-volume
              mountPath: /models
            - name: download-script
              mountPath: /scripts
      containers:
        - name: api
          image: {{ .Values.registry.url }}/{{ .Values.bankchurn.image }}
          imagePullPolicy: {{ .Values.registry.pullPolicy }}
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: MODEL_PATH
              value: {{ .Values.bankchurn.model.localPath }}
          resources:
            {{- toYaml .Values.bankchurn.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
          volumeMounts:
            - name: model-volume
              mountPath: /models
      volumes:
        - name: model-volume
          emptyDir: {}
        - name: download-script
          configMap:
            name: download-script
---
apiVersion: v1
kind: Service
metadata:
  name: bankchurn-service
  namespace: {{ .Values.namespace }}
  labels:
    app: bankchurn-predictor
spec:
  selector:
    app: bankchurn-predictor
  ports:
    - port: 80
      targetPort: 8000
      name: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: bankchurn-hpa
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: bankchurn-predictor
  minReplicas: {{ .Values.bankchurn.hpa.minReplicas }}
  maxReplicas: {{ .Values.bankchurn.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.bankchurn.hpa.cpuTarget }}
{{- end }}
