{{- if .Values.carvision.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: carvision-market-intelligence
  namespace: {{ .Values.namespace }}
  labels:
    app: carvision-market-intelligence
    {{- include "ml-portfolio.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.carvision.replicas }}
  selector:
    matchLabels:
      app: carvision-market-intelligence
  template:
    metadata:
      labels:
        app: carvision-market-intelligence
    spec:
      initContainers:
        - name: download-model
          image: {{ .Values.modelStorage.initImage }}
          command: ["python", "/scripts/download-model.py"]
          env:
            - name: GCS_BUCKET
              value: {{ .Values.modelStorage.bucket }}
            - name: GCS_MODEL_PATH
              value: {{ .Values.carvision.model.gcsPath }}
            - name: LOCAL_MODEL_PATH
              value: {{ .Values.carvision.model.localPath }}
          volumeMounts:
            - name: model-volume
              mountPath: /models
            - name: download-script
              mountPath: /scripts
      containers:
        - name: api
          image: {{ .Values.registry.url }}/{{ .Values.carvision.image }}
          imagePullPolicy: {{ .Values.registry.pullPolicy }}
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: MODEL_PATH
              value: {{ .Values.carvision.model.localPath }}
          resources:
            {{- toYaml .Values.carvision.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
          volumeMounts:
            - name: model-volume
              mountPath: /models
      volumes:
        - name: model-volume
          emptyDir: {}
        - name: download-script
          configMap:
            name: download-script
---
apiVersion: v1
kind: Service
metadata:
  name: carvision-service
  namespace: {{ .Values.namespace }}
  labels:
    app: carvision-market-intelligence
spec:
  selector:
    app: carvision-market-intelligence
  ports:
    - port: 80
      targetPort: 8000
      name: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: carvision-hpa
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: carvision-market-intelligence
  minReplicas: {{ .Values.carvision.hpa.minReplicas }}
  maxReplicas: {{ .Values.carvision.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.carvision.hpa.cpuTarget }}
{{- end }}
