# MLflow Infrastructure Environment Variables
# Copy this file to .env and fill with secure values
# DO NOT commit .env to git!

# PostgreSQL credentials for MLflow backend
POSTGRES_PASSWORD=your_secure_password_here_use_openssl_rand

# MinIO credentials for artifact storage
MINIO_ROOT_PASSWORD=your_secure_minio_password_here

# AWS S3 compatible credentials (same as MinIO for local)
AWS_SECRET_ACCESS_KEY=your_secure_minio_password_here

# Generate secure passwords with:
# openssl rand -base64 32
