# BankChurn Predictor — Production runtime dependencies (inference only)
# Pinned with compatible-release (~=) for reproducibility
# Runtime: Python 3.11 | Model: StackingClassifier (RF+GB+XGB+LGB→LR)
# SHAP, MLflow, imbalanced-learn are NOT needed at inference time.

# Core Data Science
pandas~=2.2.0
numpy~=1.26.0
scipy~=1.13.0

# ML Frameworks (inference)
scikit-learn~=1.6.0
# xgboost + lightgbm installed separately in Dockerfile with --no-deps
joblib~=1.4.0

# API Framework
fastapi~=0.135.1
uvicorn~=0.34.0
pydantic~=2.10.0
python-multipart~=0.0.9

# Configuration
pyyaml~=6.0.0

# Monitoring
prometheus-client~=0.21.0

# Type hints
typing-extensions~=4.12.0
