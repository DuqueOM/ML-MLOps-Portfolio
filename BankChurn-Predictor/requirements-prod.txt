# BankChurn Predictor — Production runtime dependencies (inference + explainability)
# Pinned with compatible-release (~=) for reproducibility
# Runtime: Python 3.11 | Model: StackingClassifier (RF+GB+XGB+LGB→LR)
# SHAP included for real-time feature contributions (trade-off: +150MB image, +200ms latency)

# Core Data Science
pandas~=2.3.0
numpy~=2.2.0
scipy~=1.14.0

# ML Frameworks (inference)
scikit-learn~=1.8.0
# xgboost + lightgbm installed separately in Dockerfile with --no-deps
joblib~=1.4.0

# Explainability (SHAP for feature contributions)
shap~=0.46.0

# API Framework
fastapi~=0.115.0
uvicorn~=0.34.0
pydantic~=2.10.0
python-multipart~=0.0.9

# Configuration
pyyaml~=6.0.0

# Monitoring
prometheus-client~=0.21.0

# Type hints
typing-extensions~=4.12.0
