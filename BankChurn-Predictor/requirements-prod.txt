# BankChurn Predictor — Production runtime dependencies only
# Used by Dockerfile for minimal image size (~400MB vs ~3GB with full deps)

# Core Data Science
pandas>=1.3.0
numpy>=1.21.0
scipy>=1.7.0

# ML Frameworks (inference)
scikit-learn>=1.8.0
# xgboost installed separately in Dockerfile with --no-deps (avoids nvidia-nccl-cu12)
imbalanced-learn>=0.10.0
joblib>=1.1.0

# API Framework
fastapi>=0.78.0
uvicorn>=0.18.0
pydantic>=1.9.0
python-multipart>=0.0.5

# Configuration
pyyaml>=6.0

# Monitoring
prometheus-client>=0.17.0

# NOTE: shap excluded from prod — pulls torch/CUDA (~2GB).
# explainability.py degrades gracefully when shap is absent.

# HTTP (healthcheck, callbacks)
requests>=2.27.0

# Type hints
typing-extensions>=4.0.0
