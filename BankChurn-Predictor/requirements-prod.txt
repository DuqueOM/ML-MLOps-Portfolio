# BankChurn Predictor — Production runtime dependencies (inference only)
# Optimized for minimal Docker image size (<500MB)
# SHAP, MLflow, imbalanced-learn are NOT needed at inference time.

# Core Data Science
pandas>=1.3.0
numpy>=1.21.0
scipy>=1.7.0

# ML Frameworks (inference)
scikit-learn>=1.8.0
# xgboost + lightgbm installed separately in Dockerfile with --no-deps
joblib>=1.1.0

# API Framework
fastapi>=0.78.0
uvicorn>=0.18.0
pydantic>=1.9.0
python-multipart>=0.0.5

# Configuration
pyyaml>=6.0

# Monitoring
prometheus-client>=0.17.0

# Type hints
typing-extensions>=4.0.0
