# CarVision Market Intelligence — Production runtime dependencies (inference only)
# Optimized for minimal Docker image size (<400MB)
# Streamlit/plotly/pyarrow NOT included — Streamlit runs as separate sidecar.

# Core Data Science
pandas>=1.3.0
numpy>=1.21.0
scipy>=1.7.0
scikit-learn>=1.8.0
# lightgbm installed separately in Dockerfile with --no-deps
joblib>=1.2.0

# API Framework
fastapi>=0.78.0
uvicorn>=0.18.0
pydantic>=1.10.0

# Configuration
pyyaml>=6.0

# Monitoring
prometheus-client>=0.17.0
