# CarVision Market Intelligence — Production runtime dependencies (inference only)
# Pinned with compatible-release (~=) for reproducibility
# Runtime: Python 3.11 | Model: LightGBM (n_estimators=500)
# Streamlit/plotly/pyarrow NOT included — Streamlit runs as separate sidecar.

# Core Data Science
pandas~=2.3.0
numpy~=2.4.0
scipy~=1.17.0
scikit-learn~=1.8.0
# lightgbm installed separately in Dockerfile with --no-deps
joblib~=1.3.0

# API Framework
fastapi~=0.115.0
uvicorn~=0.34.0
pydantic~=2.10.0

# Configuration
pyyaml~=6.0.0

# Monitoring
prometheus-client~=0.21.0
