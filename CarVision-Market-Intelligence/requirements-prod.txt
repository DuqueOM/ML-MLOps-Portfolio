# CarVision Market Intelligence — Production runtime dependencies only
# Used by Dockerfile for minimal image size

# Core Data Science
pandas>=1.3.0
numpy>=1.21.0
scipy>=1.7.0
scikit-learn>=1.8.0
# xgboost installed separately in Dockerfile with --no-deps (avoids nvidia-nccl-cu12)
joblib>=1.2.0

# API Framework
fastapi>=0.78.0
uvicorn>=0.18.0
pydantic>=1.10.0

# Dashboard (Streamlit sidecar)
streamlit>=1.28.0
plotly>=5.0.0

# Data Processing
pyarrow>=8.0.0

# Configuration
pyyaml>=6.0
tqdm>=4.62.0

# Monitoring
prometheus-client>=0.17.0

# HTTP
requests>=2.27.0
